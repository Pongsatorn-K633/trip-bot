# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── LINE Messaging API ────────────────────────────────────────────────────────
line-bot-sdk>=3.12.0

# ── LLM Client ────────────────────────────────────────────────────────────────
openai>=1.0.0          # Typhoon API (OpenAI-compatible)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
httpx>=0.27.0          # Required internally by line-bot-sdk v3

# ── Phase 2 (ML NER) — dev_tools only, not needed on server ──────────────────
# Install locally: pip install transformers torch sentencepiece protobuf tiktoken
# transformers>=4.40.0
# torch>=2.2.0
# sentencepiece>=0.1.99
# protobuf>=4.0.0
# tiktoken>=0.6.0
# pythainlp>=5.0.0
# accelerate>=0.27.0    # fine-tuning only
